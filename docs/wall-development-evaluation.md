# AeroWall 壁球开发评测

本轮完成 128 环境 × 25 次 PPO 更新，共 204,800 个新增环境交互步。包含垫球预训练后累计为 32,972,800 步。训练日志记录 538 次已结束 episode、55 次墙面接触、1 次完整接回、0 次目标成功回合；训练中的一次接回不能作为独立成功率。

新增独立评测脚本 `scripts/evaluate_wall_rally.py`：从固定的 100 个发球场景开始，只统计每个槽位首次 episode，保存原始碰撞事件和轨迹。其后重置仅维持仿真运行，不拼接成绩。当前每场 10 秒，属于开发评测，不替代计划中的正式 60 秒评测。

早期 checkpoint（新增 8,192 步）评测完成：接回率 0/100，92 场到达时限、4 场无人机碰墙、2 场越界、2 场非拍面碰撞。结果表明主要行为仍是原地垫球，尚未实现壁球开发目标。

`scripts/audit_wall_evaluation.py` 不导入环境或回合状态机，独立按物理时间排序碰撞事件、重建回合。100 场的回合数、最长连续回合、墙面接触与合法拍面接触数量全部一致，事件时钟验证通过。这验证计数一致性，不单独证明碰撞几何准确性。

末期 checkpoint 同场景评测已完成：接回率仍为 0/100，81 场到达时限、11 场越界、6 场球碰机体、1 场无人机碰墙、1 场非拍面碰撞。独立事件计数核验也通过。

末期共有 1,357 次合法拍面接触，其中仅 3 次碰撞后的向墙速度超过 1 m/s。向墙速度的 10%、50%、90% 分位约为 -0.090、-0.0003、0.091 m/s；多数动作仍在原地垫球。5 个实际碰墙场景全部未接回。首先需要改善送墙，再检验接回与恢复，不能把当前失败仅归因于接球。

新增开发课程 `configs/wall_launch_curriculum.yaml`：只在真实合法拍面碰撞时，对当前目标对应的出球速度提供平滑奖励。期望飞行时间暂设 0.35 秒，三个速度尺度为 [1.5, 1.5, 2.0] m/s。奖励扣除保持当前其余速度、仅将向墙速度置零的参考分数，因此纯垂直出球的该项奖励为零；负向出球可得到负值。该计算仅用于奖励，不修改物理速度、不暴露未来信息给 actor、不代替实际碰墙或接回计分。

新课程的 16 环境两次更新短训通过，18 项现有单元测试通过。`wall-launch-dev-001` 已启动：与前一轮同初始策略、同 128 环境和 204,800 步预算，新增记录 launch shaping 正负次数与向墙速度总和。其效果尚待真实评测，配置仍为开发版本。三方法三种子比较、正式评测、消融及演示仍未完成。

证据文件：`wall-rally-dev-001.json`、`wall-rally-eval-early-01.json`、`wall-rally-eval-early-audit.json`、`wall-rally-eval-final-01.json`、`wall-rally-eval-final-audit.json`、`wall-launch-diagnosis.json`。服务器项目内保留对应日志、checkpoint、原始事件和轨迹。

## 实际碰墙失败轨迹

新增 `scripts/plot_wall_attempt.py`，从首次 episode 的保存轨迹和物理碰撞时刻生成出球—碰墙—后续运动图，不拼接重置。场景 71 在 1.470 秒合法击球，2.025 秒碰墙，2.060 秒结束，没有下一次合法接球。出球速度约为 [1.264, -2.585, 1.962] m/s；最终球位置约 [0.825, -1.806, 0.698] m，越过 y=-1.8 m 的侧边界。无人机同期并未越界。因此该场景还揭示了出球横向偏差，不能仅以“已经碰墙”说明完成正确送球。

![实际碰墙失败轨迹](wall-attempt-scenario71.png)

对应 `wall-attempt-scenario71.json` 保存微步接触法向、拍面接触点速度、碰前碰后球速及失败结果。图中的机身姿态曲线为策略步采样，碰撞时刻信息来自物理子步；两者采样频率不同。

新增评测选项 `--initialize-juggle`，先验证原始 24 维策略到 43 维 actor 的输出等价，再评测零壁球更新的起点。`wall-rally-eval-untrained-01` 已完成且事件核验通过：100 场有 1,560 次合法拍面接触，0 场球碰墙、0 场接回；95 场到时限、3 场球碰机体、1 场越界、1 场无人机碰墙。

可重复比较入口 `scripts/compare_wall_evaluations.py` 校验场景哈希、物理/策略步长、时限、墙体几何、目标序列、目标半径及场地边界相同后输出比较。其结果保存为 `wall-development-comparison.json`。当前比较表明旧课程由零碰墙增加到 5/100 场碰墙，但接回仍为 0/100；这不是正式三种子结果。

新送墙课程 `wall-launch-dev-001` 的 25 次更新已完成：204,800 新增步、696 个结束 episode、143 次墙面接触、0 次接回，数值和 checkpoint 保存正常。训练期 episode 长度不同，不能直接用这组总数与旧课程比较成功率。固定场景评测 `wall-launch-eval-25-01` 已启动，待结果与独立事件核验。

同配置续训 `wall-launch-dev-002` 已启动，恢复已有学习状态，额外 100 次更新（819,200 步），每 25 次保存 checkpoint。两轮合计壁球训练预算将为 1,024,000 步，连同原始垫球预训练将为 33,792,000 步；这是计划预算，不表示尚在运行的续训已完成。整体实施目标保持未完成。

## 送墙课程首轮固定评测结果

`wall-launch-eval-25-01` 已完成，进程退出和事件重建核验均通过。三组使用同一批 100 个首次发球场景、相同物理步长、目标几何和 10 秒时限：

| 策略 | 球碰墙场景 | 完整接回场景 | 合法拍面接触 | 向墙出球速度 >1 m/s 的接触 |
| --- | ---: | ---: | ---: | ---: |
| 原始垫球策略 | 0 | 0 | 1560 | 1 |
| 旧奖励，新增 204800 步 | 5 | 0 | 1357 | 3 |
| 送墙速度课程，新增 204800 步 | 12 | 0 | 1137 | 16 |

新课程的 100 场终止分布为：68 场时间上限、14 场越界、12 场无人机触地、3 场球碰机体、3 场非拍面接触。12 个碰墙场景中，7 个最终无人机触地，5 个越界。碰墙增加伴随稳定性损失，不能称为接回能力改善或任务达标。

场景 27 的实际轨迹显示：0.310 秒合法击球、0.685 秒碰墙；无人机击球后倾斜并持续失高，物理第 371 步（0.9275 秒）触地终止，没有接回。图中最后一次策略步采样已在触地之后，因此末端角速度峰值包含地面碰撞影响，不能全归因于此前的控制动作。

![送墙后触地失败轨迹](wall-launch-attempt-scenario27.png)

当前证据支持继续关注击球后的恢复，而非单纯提高送墙奖励。续训仍保持原配置，以观察更长预算下是否出现真实接回；如恢复失败持续，再单列阶段性恢复奖励或真实接触后状态课程。不会增加全程小倾角限制或通过重设球状态制造成功。

新增 `scripts/analyze_wall_recovery.py`，按真实碰撞时刻截取出球后的机身状态，严格排除终止物理子步及之后的策略步采样，避免将地面撞击造成的角速度误判为此前控制失稳。诊断覆盖上述全部 12 个碰墙场景，11 个从首次碰后采样到最后终止前采样失高超过 0.3 米。场景 61、64 在最后一次碰墙前采样时无人机高度分别约 0.35、0.30 米，碰墙后仅约 0.055、0.035 秒即终止。诊断没有新增恢复成功阈值，也没有将静态悬停定义为壁球恢复成功。

数据保存于 `wall-launch-recovery-analysis.json`。续训已保存累计 50 次壁球更新、33,177,600 总交互步的 checkpoint；已启动 `wall-launch-eval-50-01` 固定场景评测，后续比较其接回与恢复表现。100 次续训预算仍在执行，尚未完成。

## 50 次更新的结果与下一开发课程

累计 50 次壁球更新的评测及事件重建核验已完成：100 场仅 2 场球碰墙、0 场接回；80 场时间上限、8 场无人机触地、6 场越界、4 场非拍面接触、2 场球碰机体。碰墙较 25 次更新的 12 场减少，不能称为训练进步。

恢复诊断新增实际 CTBR 动作映射：上游先 tanh，再将最后一维映射为 0–15 m/s² 的集体加速度指令。25 次更新评测的多个失败场景在击球后的该指令中位数接近零（例如场景 22、46、61、76、83、90）。这说明失高不只与倾斜有关。指令与策略步结束姿态的垂直投影只是诊断，不代表实测推力或净加速度；转子响应、混控限幅与模型质量均可能影响实际受力。

新增独立开发配置 `wall_outbound_curriculum.yaml`：在原送墙速度课程基础上，为真实、无歧义的“合法拍面击球→一次墙面碰撞”增加 25 分课程奖励。裸墙碰撞、连续两次墙碰撞、同子步拍墙顺序歧义和非法接触不获此奖励。奖励后继续原 episode；完整接回仍必须经历下一次合法拍面碰撞，仍奖励 100 分，不修改回合成功率。该版本用于克服策略回到垫球的局部行为，不能用于宣称已学会接回；短训 `wall-outbound-smoke-01` 已通过，2048 步中记录 3 次合法送墙、0 次接回。新增 `wall-task-contract-02` 正在验证无先前合法击球的墙碰撞不获课程奖励。

此前 100 次续训仍以原课程运行，保留作为更长预算证据。新课程不会替换正在运行进程的已加载代码或配置；各运行记录使用各自的源码哈希。

`wall-task-contract-02` 已通过：16 次裸墙碰撞均未获得送墙课程奖励，目标切换、超时、选择性重置继续通过。`wall-outbound-dev-001` 已从同一原始垫球 checkpoint 启动，128 环境、25 次更新、204800 步，与此前短课程预算一致，结果待评测。

新增 `scripts/source_evidence.py`：后续训练和评测在启动时把已列入哈希清单的源码原始字节保存到运行对应的 `.sources/` 目录，并在仿真初始化结束、开始收集或评测之前再次核对文件哈希。启动期间发生修改则报错，防止初始化耗时较长时混用版本。已验证字节留档和修改拒绝路径，脚本语法检查通过。该措施只用于后续新运行；不会补造既有运行的源码快照，也不覆盖已加载代码的运行进程。

原送墙课程续训 `wall-launch-dev-002` 已完成额外 819200 步，进程退出与报告状态均通过。本段累计 3297 个结束 episode、439 次球碰墙、0 次完整接回；连同前段合计壁球训练 1024000 步，总预算计数 33792000 步。末期 checkpoint 已保存，`wall-launch-eval-125-01` 固定场景评测已启动，尚待结果。不能把训练预算完成视为接回门槛通过；“击球后恢复”仍是未解决的关键问题。

## 125 次更新评测与恢复控制器对照

125 次更新的固定评测已完成且事件核验通过：7/100 场球碰墙，0/100 接回；69 场时限、13 场越界、6 场无人机触地、5 场无人机碰墙、4 场非拍面碰撞、3 场球碰机体。25、50、125 次更新的碰墙场景分别为 12、2、7，接回均为零。该奖励下延长预算未达到开发目标。

新增 `aerowall/learning/recovery_controller.py` 作为计划中的控制器诊断：学习策略先完成真实合法向前击球；检测到可观测出球阶段且球 vx>0.5 m/s 后，改用弹道落点预测和位置 PD 生成同一 CTBR 接口命令。使用固定 0.8 恢复系数先验，不读取仿真真实材质；无人机目标高度暂设 1 米，指令遵循原角速度和集体推力范围。每个 episode 的切换状态在重置时清空。

该对照仅输出动作，不写球或无人机物理状态，初步测试验证有限动作、切换/重置和输入状态不被修改。`wall-hybrid-recovery-01` 使用原 25 次更新送墙策略，在同一 100 场景上验证学习出球后能否恢复。它明确标记 `hybrid_ballistic_pd_recovery`，不能作为 FF/GRU/Estimator 学习方法成绩；轨迹逐步保存控制器接管掩码。

新增合法送墙奖励的 `wall-outbound-dev-001` 也已训练结束，其纯策略评测 `wall-outbound-eval-25-01` 已启动。两项结果均待核验，不以控制器实现或启动评测代替实际成功证据。

两项评测现已完成，事件核验均通过：合法送墙奖励课程的纯策略有 26/100 场碰墙、0/100 接回，30 场无人机碰墙，任务尚未达标。其余结束原因为 26 场时限、18 场越界、13 场非拍面碰撞、10 场球碰机体、3 场无人机触地。

原 25 次更新送墙策略加 PD 恢复对照中，22 个首次 episode 发生控制器接管，共 895 个计分策略步由控制器执行。15/100 场碰墙，0/100 接回；这些碰墙场景均未出现超过 0.3 米的碰后失高，14 场因球 z 低于场地边界结束、1 场球碰机体。控制器解决了部分失高，但没有实现拦截。场景 27 的球返回接球高度时，球与机体横向位置相差约 0.465 米。

新增 `--recovery-guidance terminal` 诊断，用估计剩余飞行时间计算终端位置/零速度的水平加速度需求，再通过原 CTBR 接口与限幅执行，以检查 PD 跟踪滞后。`wall-hybrid-terminal-01` 已启动，仍使用固定 0.8 材质先验，明确属于模型控制对照，不是学习方法评测；其结果尚待验证。

终端拦截对照现已完成并通过事件核验，12/100 场碰墙、0/100 接回；结束原因为 68 场时限、12 场无人机碰墙、8 场越界、7 场球碰机体、4 场非拍面碰撞、1 场无人机触地。它未解决接回，并引入更多无人机碰墙，因此不能视为有效恢复方案。

保留两个控制器对照负结果。新增合法送墙奖励的纯学习课程已启动 `wall-outbound-dev-002`，从已完成 25 次更新的 checkpoint 恢复，再训练 100 次更新（819200 步），每 25 次保存 checkpoint。该续训使用源码快照与初始化版本核验；结果仍待实际训练和固定场景评测，不预先声称接回改善。

`wall-outbound-eval-50-01` 已完成并通过事件核验：累计 50 次壁球更新的策略在 100 场中有 31 场碰墙、0 场接回。结束原因为 38 场时限、26 场无人机碰墙、15 场越界、12 场非拍面碰撞、9 场球碰机体。训练记录中的偶发接回不能替代这个固定评测结果。续训第 45 次更新时本段累计已有 7 次接回、997 次碰墙，但尚未完成本段预算，也未证明稳定接回能力。

## 75 次更新：首次固定场景接回

`wall-outbound-eval-75-01` 的 100 个固定开发场景全部结束，独立事件链与时钟核验通过：48 场球碰墙、1 场完成一次物理接回、0 场目标命中接回、0 场连续 10 次接回。结束原因为 33 场球碰机体、35 场越界、14 场无人机碰墙、15 场非拍面接触、3 场无人机触地，已无时间上限结束场景。碰墙增多伴随更快失败，不能据此称为稳定控制改善。

唯一接回为场景 68：合法出球 0.9325 秒、墙碰撞 1.1575 秒、下一次合法拍面接触 1.3525 秒，1.715 秒无人机触地。该场只有一次物理接回，没有目标命中；这是同一真实 episode 的事件记录，不拼接重置轨迹。checkpoint 总交互步 33,382,400，其中壁球阶段 614,400 步，仍属于单种子、10 秒开发评测。

审计保存于 `wall-outbound-eval-75-audit.json`，同场景对比保存于 `wall-outbound-comparison-75.json`，轨迹与时刻保存于 `wall-outbound-first-return-68.png/.json`。续训仍在执行原定 100 次追加更新，尚未达到单次接回 70% 的开发门槛。

![场景68的首次接回及后续触地](wall-outbound-first-return-68.png)


75 次更新的碰后诊断覆盖全部 48 个碰墙场景，其中 42 个从出球后首个采样到终止前最后采样失高超过 0.3 米。场景 68 从约 1.133 米下降到 0.207 米，终止前下降速度约 2.44 m/s；集体加速度指令中位数约 7.60 m/s²。诊断严格排除触地物理子步及之后的采样，不把碰地冲击解释为此前控制失稳。首个接回图已补标接回时刻并排除终止冲击采样，数据在 `wall-outbound-recovery-75.json`。

评测入口新增 `actor_observation_before`：逐策略步保存实际输入的 43 维观测，并记录速度观测延迟设置及采样时序。它在动作和物理推进之前保存，与既有物理状态轨迹的步后采样分开；后续显式估计器须以可见历史为输入。新增 `audit_actor_observation.py` 核对首次 episode 的位置和零延迟速度时序，延迟不为零时明确跳过速度核验。累计 100 次壁球更新的评测 `wall-outbound-eval-100-01` 已启动，用于实际验证记录与核验脚本，尚未获得结果。


累计 100 次壁球更新的固定评测已完成：44/100 场碰墙、0/100 场接回；60 场无人机碰墙、16 场越界、13 场非拍面接触、11 场球碰机体。事件重建核验通过，但未重现 75 次更新时的单场接回，说明目前策略尚不稳定。新增观测日志共覆盖 8376 个计分策略输入；位置和零延迟速度时序核验通过，最大误差为相对球位置浮点差约 5.96e-8，其余检查为零。数据见 `wall-outbound-comparison-100.json`、`wall-outbound-observation-100-audit.json`。


## 合法送墙课程完成与奖励平衡对照

`wall-outbound-dev-002` 已正常完成额外 819200 个环境交互步，总预算为 33792000 步（壁球阶段累计 1024000 步）。本段 8972 个结束 episode 中记录 3430 次碰墙、35 次物理接回、0 次目标命中接回；这些是训练采样统计。

最终累计 125 次壁球更新的固定评测已完成并通过事件与观测时序核验：54/100 场碰墙，0/100 场接回；53 场无人机碰墙、19 场越界、18 场非拍面接触、10 场球碰机体。25/50/75/100/125 次更新的接回场景数为 0/0/1/0/0，不能称为稳定接回。完整对比见 `wall-outbound-comparison-125.json`。

训练奖励诊断发现 3379 个仅碰墙一次、未接回即失败的 episode 全部获得正回报，平均约 6.19 分。原课程一次合法送墙奖励 25 分、失败扣 20 分，为快速失败留下了正收益路径。这是具体奖励缺陷证据，但不能单独证明所有控制问题都由该项造成；完整统计见 `wall-outbound-reward-diagnosis.json`。

新增独立开发配置 `wall_recovery_curriculum.yaml` 将送墙奖励降到 5 分，保留完整接回奖励 100 分、失败扣 20 分及其他物理/计分条件。单次送墙后失败的稀疏部分从 +5 变为 -15，不改变评测成功定义。该对照暂不增加全程姿态约束。

训练入口新增已有 43 维 Wall actor 的仅策略迁移，保留全部先前环境步数，重新初始化 critic、value normalizer 和优化器，更新计数重新开始。已有 24 维 Juggle actor 迁移继续保留。以曾产生一次固定场景接回的累计 75 次更新 checkpoint 为开发起点，`wall-recovery-smoke-01` 正在验证 16 环境、2 次更新的迁移与原生训练路径；不能将课程修改视为问题已解决。


`wall-recovery-smoke-01` 现已通过进程退出和报告双重核验。64 组随机观测的迁移前后动作差为零，随后完成 2048 个环境交互步；本次仅策略迁移累计预算保留为 33384448 步。`wall-recovery-dev-001` 已从原累计 75 次壁球更新 checkpoint 独立启动 128 环境、100 次更新、819200 步的开发训练，每 25 次保存 checkpoint。它不从短训结果继续，避免将 16 环境试跑混入正式开发课程；仍未证明接回改善。


## HCSP 启发的阶段恢复对照

新增 `phase_recovery.py` 和独立 `wall_phase_recovery_curriculum.yaml`，借鉴固定版本 HCSP `Receive_hover.py` 的击球后位置/朝上/旋转恢复项。这里回位目标为局部坐标 [0,0,1]，只作为恢复技能开发对照，不是预测接球位置或真实接回定义。

奖励仅在合法拍面出球后的 TO_WALL 且 vx>0.5 m/s，或合法单次碰墙后的 TO_BAT 阶段启用；终止/截断状态不启用。使用位置项 1/(1+(1.2d)^2)、朝上项 ((up_z+1)/2)^2、三轴角速度项 1/(1+||omega||²)，组合为 pose*(1+up+rotation)，分数范围 0–3，再乘 3 和策略时间步 0.02 秒，最高每秒 9 分。与 HCSP 的差异包括三轴旋转项、按时间步缩放和壁球阶段门控；不能称为直接复现。未增加全程姿态限制，真实物理接触与回合评分保持原定义。

解析契约检查覆盖阶段隔离、终止屏蔽、姿态/位置/旋转响应、输入不修改和分数有界。`wall-phase-recovery-smoke-01` 已启动 16 环境、2 次更新的原生集成验证，结果待核验。奖励平衡课程仍继续训练，其第 25 次更新 checkpoint 的固定评测 `wall-recovery-eval-25-01` 已启动，结果待核验。


阶段恢复短训现已完成 2048 步，进程退出和报告均通过，初始化源码一致性通过。已从同一原累计 75 次更新 actor 独立启动 `wall-phase-recovery-dev-001`，128 环境、25 次更新、204800 个新增环境步，便于与奖励平衡课程前 25 次更新做同起点、同预算开发对比。实际效果待固定场景评测。


奖励平衡课程第 25 次更新固定评测已完成并通过事件重建：13/100 场球碰墙、0/100 场接回、944 次合法拍面接触；结束原因为 48 场时限、14 场越界、10 场无人机碰墙、15 场球碰机体、11 场非拍面接触、2 场无人机触地。相比旧送墙课程在同累计环境步数下的 44 场球碰墙、60 场无人机碰墙，新课程更偏向垫球，不能据此声称接回改善。两支训练的 critic/优化器处理不同，因此该对比是开发描述，不是单变量奖励消融。

13 个碰墙场景中，9 个出球后失高超过 0.3 米；10 个最终无人机碰墙。相关证据保存于 `wall-recovery-eval-25-audit.json`、`wall-recovery-comparison-25.json`、`wall-recovery-analysis-25.json`。第 50 次更新 checkpoint 的同场景评测 `wall-recovery-eval-50-01` 已启动。阶段恢复课程与奖励平衡课程均从同一 actor、相同新 critic/优化器初始化流程开始，前 25 次更新可用于更直接的恢复项对照，结果仍待评测。


新增 `analyze_return_approach.py`，在首次墙碰撞之后、下一次合法拍面接触或终止之前，统计球与无人机刚体原点的距离、相对位置和速度。明确排除终止冲击采样；该距离不是拍面表面距离，20 ms 采样也不能代替 2.5 ms 接触事件判定。

奖励平衡第 25 次更新的 13 个碰墙场景中，最小原点距离的 10%/50%/90% 分位为 0.201/0.305/0.563 米；5 场最接近球的采样恰为终止前最后一次采样，分别 3 场无人机碰墙、1 场球碰机体、1 场无人机触地。不能仅据距离把失败全部归因于拦截延迟，飞行碰撞提前终止也会截断接近过程。

对照此前唯一接回的场景 68：墙后到下一合法拍面接触之前共有 10 个采样，1.34 秒时球与机体原点相距约 0.157 米，径向相对速度约 -2.168 m/s，随后 1.3525 秒发生真实合法接回。接触是否有效仍以独立事件审计为准。证据为 `wall-recovery-approach-25.json`、`wall-outbound-approach-75.json`。


奖励平衡第 50 次更新固定评测完成并通过事件审计：4/100 场碰墙、0/100 接回、1269 次合法拍面接触，81 场时限、8 场越界、4 场非拍面接触、3 场球碰机体、2 场无人机触地、2 场无人机碰墙。相较第 25 次更新，进一步回到垫球行为；减少失败没有带来壁球任务成功。证据为 `wall-recovery-eval-50-audit.json` 和 `wall-recovery-comparison-50.json`。本课程仍完成既定预算，以保留完整负结果；阶段恢复对照仍在训练，尚无固定评测结论。


奖励平衡第 75 次更新评测完成并通过事件审计：2/100 场碰墙、0/100 接回、1090 次合法拍面接触；75 场时限、10 场球碰机体、7 场越界、5 场非拍面接触、2 场无人机触地、1 场无人机碰墙。该课程继续趋向垫球，尚未达到单次接回门槛。阶段恢复课程 `wall-phase-recovery-dev-001` 已完成 25 次更新并通过退出/报告核验，其固定评测 `wall-phase-recovery-eval-25-01` 已启动。独立恢复技能课程仍在训练。


固定回位阶段奖励的单策略课程第 25 次更新评测已完成并通过事件审计：32/100 场球碰墙、0/100 接回，712 次合法拍面接触；32 场球碰机体、20 场时限、17 场越界、9 场非拍面、17 场无人机碰墙、5 场无人机触地。32 个碰墙场景中 21 场出球后失高超过 0.3 米，尚未解决恢复问题。

奖励平衡课程完成全部 100 次更新（新增 819200 步），最终固定评测通过事件审计：7/100 场碰墙、0/100 接回，903 次合法拍面接触；63 场时限、6 场触地、6 场无人机碰墙、10 场球碰机体、5 场越界、10 场非拍面。该完整负结果说明目前课程仍偏向垫球，没有达到单次接回门槛。独立恢复技能及预测参考对照正在同预算训练/评测。


独立恢复技能 `recovery-skill-eval-25-01` 已完成固定 100 场景评测并通过进程/报告及独立事件链审计，实际启用分阶段 checkpoint 推理：48/100 场碰墙、0/100 接回、173 次合法拍面接触。结束原因为 52 场越界、8 场无人机碰墙、16 场非拍面接触、24 场球碰机体。48 个碰墙场景中，42 个出球后失高超过 0.3 米。相较同预算固定回位单策略的 32 场碰墙，分阶段策略保留了更多出球，但尚未改善接回。不能将训练期间的 13 次接回等同于固定评测成功率。

预测参考独立恢复技能 `predictive-recovery-dev-001` 已完成 25 次更新、204800 新增环境步，进程与报告均通过；其同场景评测 `predictive-recovery-eval-25-01` 已启动，尚无结果。证据：`recovery-skill-eval-25-audit.json`、`recovery-skill-analysis-25.json`、`recovery-skill-comparison-25.json`。
