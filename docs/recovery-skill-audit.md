# AeroWall 独立恢复技能检查

参考固定版本 HCSP 的 `mappo_Receive_hover.py`：仅更新 `policy_FirstPass_hover`，并通过 `FirstPass_hit` 掩码训练恢复技能。AeroWall 新增 `RecoverySkillPolicy` 作为开发对照，不把它的结果冒充原计划三种单策略方法的正式成绩。

固定出球 actor 来自累计 75 次壁球更新的 checkpoint，恢复 actor 从相同权重初始化，critic 和优化器重新建立。动作执行前，仅用实际 actor 观测中的阶段与球 vx 选择策略：TO_WALL 且 vx>0.5 m/s，或 TO_BAT 时使用恢复策略，其余使用固定出球策略。没有重写球或机体状态，没有读取真实材质或未来真实轨迹。

采样动作、动作 log probability、entropy 和调试分布字段均随实际执行分支一起选择。PPO actor 更新先过滤恢复策略实际执行的行；空批次跳过 Adam，防止动量造成无样本更新。critic 和 GAE 仍覆盖整个切换策略轨迹，优势归一化沿用上游全批次做法。与 HCSP 的乘掩码全批平均不同，这里 actor 损失按选中样本平均；不是逐行照搬原实现。

`recovery-skill-smoke-01` 已通过进程/报告双重检查，共 16 环境、2 次更新、2048 个环境步：

- 第一批：88 条恢复交互、936 条固定出球交互；动作概率重算最大误差分别 8.58e-6、1.00e-5。
- 第二批：242 条恢复交互、782 条固定出球交互；对应最大误差 3.70e-5、1.13e-5。
- 空恢复批次跳过 actor 更新，参数不变；固定出球参数在整个训练中逐项保持不变。
- checkpoint 同时保存两支 actor；磁盘重载后确定性动作和分支掩码完全一致。

环境帧数包括两支策略执行的全部交互，并计入源 checkpoint 的 33382400 步，短训累计计数 33384448 步。控制阶段在物理子步中可能变化，但恢复策略只能在下一个 50 Hz 决策时刻接管；奖励门控步数与 actor 接管步数不必相同。

已启动 `recovery-skill-dev-001`：128 环境、25 次更新、204800 个新增环境步，从原出球 checkpoint 独立初始化，不继承短训更新。与阶段恢复单策略课程采用相同物理任务和恢复奖励，区别是固定出球和仅恢复 actor 更新。两者均待固定场景评测；当前没有独立恢复技能成功率结论。

评测入口已增加 checkpoint 模式识别，输出 `hierarchical_learned_recovery_skill`，逐步记录恢复分支掩码。完整评测入口的本模式尚待实际运行；原生短训已覆盖同一切换逻辑和 checkpoint 重载。


预测参考对照已加入：仅根据当前 actor 可见的球位置、速度、墙几何和阶段，以固定弹性先验 0.8 预测下降至接球高度的位置。仅接受边界内、发生于碰墙后的预测；预测无效时回退固定位置 [0,0,1]。参考的边界检查不代表动力学可达性。当前明确限制为零速度观测延迟，未使用未来真实轨迹或真实弹性标签。

`predictive-recovery-smoke-01` 完成 16 环境、2 次更新、2048 步，退出和报告均通过；固定击球参数不变、分支动作概率重算及保存重载检查通过。334 个恢复奖励生效步中仅 15 步采用预测参考，319 步回退固定位置，覆盖率较低，尚不能据此宣称接回改善。参考输入不修改、有效/无效分支和固定高度契约检查通过。

`predictive-recovery-dev-001` 已从同一原始出球 actor 启动 128 环境、25 次更新、204800 新增步的对照训练。固定位置的独立恢复技能 `recovery-skill-dev-001` 已完成相同预算并通过退出检查，固定 100 场景评测 `recovery-skill-eval-25-01` 已启动；结果待核验。这些均为开发诊断，不替代原计划三方法、多种子及 60 秒正式评测。


新增对已保存实际 actor 输入的参考覆盖分析（`phase-recovery-reference-coverage-25.json`）。固定回位单策略评测共有 1876 个恢复决策步：304 个无效弹道预测、937 个下降至参考高度发生在碰墙前、387 个预测落点超出参考边界、248 个接受参考。四类互斥且覆盖全部恢复决策步。此处为动作前采样，不能与训练中动作后奖励步数直接等同；同一场景各采样相关，不能视作 1876 次独立试验。该证据表明当前出球轨迹也限制了固定高度接球参考的适用范围，参考有效不等于无人机能够及时到达。


独立恢复技能 `recovery-skill-eval-25-01` 已完成固定 100 场景评测并通过进程/报告及独立事件链审计，实际启用分阶段 checkpoint 推理：48/100 场碰墙、0/100 接回、173 次合法拍面接触。结束原因为 52 场越界、8 场无人机碰墙、16 场非拍面接触、24 场球碰机体。48 个碰墙场景中，42 个出球后失高超过 0.3 米。相较同预算固定回位单策略的 32 场碰墙，分阶段策略保留了更多出球，但尚未改善接回。不能将训练期间的 13 次接回等同于固定评测成功率。

预测参考独立恢复技能 `predictive-recovery-dev-001` 已完成 25 次更新、204800 新增环境步，进程与报告均通过。证据：`recovery-skill-eval-25-audit.json`、`recovery-skill-analysis-25.json`、`recovery-skill-comparison-25.json`。

首次预测参考评测 `predictive-recovery-eval-25-01` 在 Orbit 导入时因 Omniverse 资产根目录连接失败而退出，未进入物理评测。Kit 退出码为 0，但报告 failed，包装器正确判为失败。保留失败报告后，以相同 checkpoint/config 启动 `predictive-recovery-eval-25-02`。运行时已本地安装，Orbit 导入仍有云资产目录探测，不能称为完全离线。

固定回位与预测参考分别启动 `recovery-skill-dev-002`、`predictive-recovery-dev-002`：各续训 75 次更新，新增 614400 步，连同前期共 819200 步；每 25 次保存检查点。配置不变，恢复学习/优化器/RNG 状态，物理 episode 重新开始。前 25 次训练分别记录 13、29 次接回，仅为训练统计，不代表固定评测成功率。

新增实际评测接管前对照：相对于原出球 checkpoint，85/100 场在首次恢复决策前的动作、球位置、机体位置逐值一致；其余场景存在小差异，最大动作差 0.002826、机体位置差 0.000639 米、球位置差 0.000458 米。旧基线未保存 actor 输入，因此不能验证全部观测相同，也不能声称全部轨迹位级一致。证据为 `recovery-skill-frozen-launch-eval-25.json`。

场景 68 的出球时刻 0.9325 秒、球速 [3.15318,0.270586,1.529792] 米/秒及碰墙时刻 1.1575 秒在两版记录中完全一致。原出球策略于 1.3525 秒合法接回，独立恢复技能版于 1.36 秒发生非拍面接触而终止。真实轨迹见 `recovery-skill-scene68.png`，接触点速度包含机体平移与旋转贡献；仅对此场景作对照，不据此推断整体因果效果。

预测参考重试评测 `predictive-recovery-eval-25-02` 已完成并通过进程/报告与独立事件链审计：48/100 场碰墙、1/100 接回、174 次合法拍面接触、0 次目标联合回合。结束原因为 29 场越界、24 场无人机碰墙、15 场非拍面、32 场球碰机体。48 个碰墙场景中 28 场失高超过 0.3 米，固定回位对照为 42 场；这只是单种子开发对照，不能证明普遍改善。

唯一接回仍在场景 68：0.9325 秒出球、1.1575 秒碰墙、1.33 秒合法接回，接回后的球速为 [1.512668,2.375955,2.858325] 米/秒，1.6525 秒第二次碰墙，1.9925 秒越界终止。相比旧出球策略该场接回后球仍远离墙面，预测参考版确实再次送墙，但没有完成第二次接回。图为 `predictive-recovery-scene68.png`，对应审计/对照/恢复分析文件分别为 `predictive-recovery-eval-25-audit.json`、`predictive-recovery-comparison-25.json`、`predictive-recovery-analysis-25.json`。当前 1% 接回率远未达到 70% 门槛。

本评测实际动作前的 2201 个恢复决策中，435 个采用有效预测参考，513 个弹道无效、935 个参考高度穿越发生在碰墙前、318 个落点越界。覆盖统计保存在 `predictive-recovery-reference-coverage-25.json`，不等同于动作后的训练奖励计数。两支相同预算续训仍在执行，后续依次评测保存的检查点。

新增 `summarize_recovery_learning.py`，按每 25 次优化更新汇总实际结束 episode，验证跨续训的更新号连续、环境步数递增及终止记录数一致；不使用旧收集器代理统计。对仍增长的日志仅读取完整行，记录已读取字节前缀的哈希，末段明确标注不完整窗口。第一份快照中，固定回位前 25 次接回 13/3022，续训第 26–38 次 9/1536；预测参考前 25 次 29/2971，续训第 26–37 次 9/1388。窗口长度不同且采样来自变化中的策略，不能据此宣称预测参考稳定领先；两者训练接回率均不足 1%。证据为 `recovery-skill-learning-snapshot-01.json` 与 `predictive-recovery-learning-snapshot-01.json`。

新增推理初始化对照 `juggle-recovery-diagnostic-01`：固定原击球 actor 和可观测阶段切换规则，仅把恢复 actor 换成已有 32768000 步垫球 checkpoint；在扩展的 43 维输入中保留前 24 维归一化，新增输入权重归零。64 组随机观测及两种新增特征尺度下，转换前后动作最大差均 4.77e-7，固定击球权重逐项不变，实际初始化检查通过，已进入固定场景评测。该双来源策略无新增训练，基座 checkpoint 和恢复来源的环境步数分别记录，未去重共同预训练祖先，因此不是等预算主方法比较。复用的 actor 转换审计文字提及训练用的新 critic，但本推理对照的 critic 仍来自基座且不决定 actor 动作。评测结果待核验。

上述初始化对照现已完成，退出、报告及独立事件链审计通过：49/100 场碰墙、2/100 场合法接回、188 次合法拍面接触、0 次目标联合回合。接回场景为 12、46，随后均因无人机碰墙终止，没有连续两次接回。全体终止原因为 33 场无人机碰墙、14 场越界、16 场无人机触地、16 场非拍面、21 场球碰机体；49 个碰墙场景中 39 场失高超过 0.3 米。与固定回位 0/100、预测参考 1/100 仅作开发描述，样本与训练来源差异不足以支持方法优劣结论。证据为 `juggle-recovery-diagnostic-audit.json`、`juggle-recovery-diagnostic-analysis.json`、`recovery-initialization-comparison.json`。

此前启动失败的路径已定位：实际配置 `use_local_usd: true`，地面和 Air 模型均选择上游本地 USD；但 Orbit `utils/assets.py` 在模块导入时调用 `get_assets_root_path()` 探测远端根目录。尚未完成全部 USD 传递依赖的离线检查，不能声称运行完全离线。重试评测成功不能证明该网络依赖已消除。

后续静态 USD 依赖检查已保存于 `local-scene-assets-inventory.json`：配置使用的地面和 Air USD、地面的三张纹理、运行时 core/Base 下的 OmniPBR.mdl 候选文件均存在，记录大小及 SHA-256；提取的 USD 引用未发现远程 URL。检查未展开 MDL 内部导入，也未证明实际材质解析器选择或扩展无网络访问，因此仍不是完整离线验证。此检查不下载、不修改资产或运行时。

两支续训均已保存累计第 50 次更新 checkpoint（33792000 个累计环境步）。`recovery-skill-eval-50-01` 已启动固定 100 场景评测，预测参考的同预算 checkpoint 待随后评测，以减少与两项续训同时竞争资源。训练仍按原定总共 100 次更新推进。

固定回位独立恢复技能的第 50 次更新评测已完成，退出/报告及独立事件审计通过：48/100 场碰墙、3/100 场合法接回、176 次合法拍面接触，0 次目标联合回合，无连续两次接回。接回场景 54、68、89 分别最终球碰机体、无人机触地、越界；场景 54 接回后再次碰墙，但未再次合法接回。全体结束原因为 31 场球碰机体、37 场越界、14 场无人机碰墙、17 场非拍面、1 场无人机触地。较第 25 次更新 0/100 有描述性进展，仍远低于 70% 门槛。

该版本 48 个碰墙场景中，46 个在出球到终止前失高超过 0.3 米。统计区间长度随 episode 改变，不应只据此数值对比姿态控制优劣。证据为 `recovery-skill-eval-50-audit.json`、`recovery-skill-comparison-50.json`、`recovery-skill-analysis-50.json`。预测参考同预算评测 `predictive-recovery-eval-50-01` 已启动，两支续训仍继续。

预测参考第 50 次更新评测已完成并通过退出/报告及事件链审计：48/100 场碰墙、4/100 合法接回、177 次合法拍面接触、0 次目标联合回合、无连续两次接回。接回场景 21、51、68、89，前三者分别无人机碰墙、碰墙、触地，89 在第二次碰墙后无人机碰墙终止。全体终止为 32 场球碰机体、30 场越界、21 场无人机碰墙、16 场非拍面、1 场触地；48 个碰墙场景中 40 场失高超过 0.3 米。4/100 相比同预算固定回位 3/100 只作开发描述，不能据一场差异宣称优越性。证据为 `predictive-recovery-eval-50-audit.json`、`predictive-recovery-comparison-50.json`、`predictive-recovery-analysis-50.json`。

新增 `audit_recovery_handoff.py` 从实际 actor 输入重建接管规则，并逐个有效采样与记录的执行分支核对一致。两版第 50 次更新均有相同的 37 个场景在恢复 actor 首次执行前失败（16 球碰机体、15 非拍面、6 越界），仅 63 个场景实际进入恢复控制。固定原出球策略而只改恢复分支，不能修复这些已记录的接管前失败；这不是普遍物理可达率上界。证据为 `recovery-skill-handoff-50.json`、`predictive-recovery-handoff-50.json`。

后续方向：完成本轮既定 100 次更新对照后，不把冻结出球分支的双策略诊断扩展为最终方法。必须改善迎球/出球阶段并回到原计划的单个目标条件策略完整回合训练；当前恢复分支对照只能提供课程与初始化依据，不能独自满足 70% 单次接回门槛。

为提前验证回到单策略的技术路径，在两支原定对照继续运行期间，使用已有 actor-only 转移入口完成 `single-recovery-transfer-smoke-01`：来源为预测恢复第 50 次更新 checkpoint 的恢复 actor，完整复制兼容的 43 维参数，64 组观测动作差为 0，critic/值归一化/优化器新建；不加载冻结出球分支。16 环境、2 次更新、2048 步已通过退出/报告检查；检查磁盘 checkpoint 确认不含 `frozen_launch_params`，累计环境步为 33794048。

已从原始来源（不继承短训更新）启动 `single-recovery-transfer-dev-001`，128 环境、25 次更新、新增 204800 步。奖励沿用预测参考课程，但所有阶段均由同一个 actor 执行和更新。此前来源的 33792000 步继续计入预算，预计结束累计 33996800 步。这是允许迎球、出球继续适应的单策略开发训练，不是双策略行为的无损合并；恢复 actor 在未训练的迎球阶段可能退化，需固定场景评测验证。

资源调度：单策略训练已通过初始化，随后以 SIGSTOP 暂时挂起并保留内存状态，让两个接近一小时运行时限的既有对照优先完成。一次性有界恢复进程检查目标 PID、进程启动时间和命令身份；两项原对照结束后发 SIGCONT，最长等待 1200 秒后也恢复，避免无限挂起。状态记在对应 `resource-pause.json` / `resource-resume.json`；不会重启训练或修改模型。该调度不暂停整体目标，包含挂起时段的训练耗时不可直接作为纯计算吞吐。

两项对照现已完成累计 100 次更新，各新增 819200 个环境步（含首轮 25 次），最终累计 34201600 步。退出与报告均通过，最终 checkpoint 重载动作及阶段掩码精确一致，冻结出球参数保持不变。固定回位四个 25 次窗口的训练接回 episode 为 13/3022、20/2994、21/3016、25/3002；预测参考为 29/2971、25/2987、27/3043、25/3092。均无目标联合回合，这些训练统计不是固定评测成功率。完整证据为 `recovery-skill-learning-100.json`、`predictive-recovery-learning-100.json`。

`recovery-skill-eval-100-01` 和 `predictive-recovery-eval-100-01` 已启动最终模型固定评测，第 75 次检查点保留待补评。自动恢复器确认两项原训练进程结束后，于记录时间 1789968098.9105248 发出 SIGCONT，状态为 resumed、原因 dependencies_ended；单策略进程保留原 PID 2945733 继续训练，没有重启。

两项最终模型评测已完成并通过进程/报告和独立事件审计。固定回位第 100 次更新：49/100 场碰墙、1/100 接回、174 次合法拍面接触；终止原因为 27 球碰机体、33 越界、24 无人机碰墙、16 非拍面。预测参考第 100 次更新：48/100 场碰墙、0/100 接回、173 次合法拍面接触；终止为 25 球碰机体、45 越界、12 无人机碰墙、16 非拍面、2 触地。分别低于各自第 50 次更新的 3/100、4/100，不能将预算完成称为性能改善。

完整第 25/50/100 次对照保存在 `recovery-skill-comparison-100.json` 与 `predictive-recovery-comparison-100.json`，对应最终事件审计为 `recovery-skill-eval-100-audit.json`、`predictive-recovery-eval-100-audit.json`。两项第 75 次补评 `recovery-skill-eval-75-01`、`predictive-recovery-eval-75-01` 已启动。既有单策略训练使用此前已选的预测参考第 50 次 actor，不替换为退化的最终模型；仍待其自身完整评测。

`single-recovery-transfer-dev-001` 已完成 25 次更新、新增 204800 步，累计 33996800 步，退出与报告通过。4754 个实际结束的训练 episode 中 405 场发生墙碰、0 场接回；终止原因为 2229 球碰机体、999 非拍面、1363 越界、77 无人机触地、86 无人机碰墙。证据为 `single-recovery-transfer-learning-25.json`。该负结果提示直接把恢复 actor 用于全部阶段可能损害迎球/出球，尚不能确认具体因果；`single-recovery-transfer-eval-25-01` 已启动相同 100 场景评测。两项第 75 次补评仍在运行。

所有补评现已完成并通过退出/报告和独立事件审计。固定回位第 25/50/75/100 次接回场景数为 0/3/1/1；预测参考为 1/4/1/0，均以 100 场为分母，均未达到门槛。第 75 次固定回位碰墙 48 场、174 次合法拍面；预测参考碰墙 46 场、175 次合法拍面。完整曲线见 `recovery-skill-comparison-complete.json`、`predictive-recovery-comparison-complete.json`。

直接转移恢复 actor 的单策略评测结果为 26/100 场碰墙、0/100 接回、133 次合法拍面接触，终止为 28 非拍面、28 越界、14 无人机碰墙、30 球碰机体；见 `single-recovery-transfer-eval-25-audit.json`。复制一个阶段的 actor 并继续这 25 次 PPO 没有保留完整两阶段行为。后续将验证使用独立新采集的训练状态、以已有两分支的动作分布为教师，拟合单个 43 维 actor 的路径；固定开发评测轨迹不作为拟合数据。只有真实单策略闭环评测通过后，才判断初始化是否改善；教师本身仅 4% 接回，拟合不会自动解决出球与恢复缺陷。
