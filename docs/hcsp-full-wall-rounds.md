# 完整 HCSP 壁球测试（目标：5–10 轮）

用户最新约束：只需要一架无人机完成壁球任务。六机排球仅作为权重/运行时诊断，不作为目标结果；不再安排三机墙面能力测试。

“完整 HCSP”保留训练好的高层网络和全部低层技能，但原系统高层输出对应三架无人机。单机部署必须显式定义观测与动作调度映射，不能把删除角色或抽取单技能称为原完整系统的直接迁移。

1. 已完成：原六机基线，核验公开权重与入口兼容性。
2. 进行中：确定性推理、步长、GPU 接触对照；这些仅服务于单机部署配置核验，不扩展成六机性能复现项目。
3. 单机适配：物理场景只有一架无人机；保留全部技能和高层网络，列明高层三角色到单机执行器的映射及观测差异。墙面先使用真实碰撞，禁止回合中写球状态。
4. 单机闭环实验：统计单机击球—墙碰撞—同机接回球；区分原权重加明确调度适配与任何训练改动。
5. 单机对照：比较原技能调度和壁球回合调度，在同一初始场景验证；不能用三机合作代替。
6. 重复验证及失败定位：不同种子/墙距/来球，独立 GPU 接触证据、轨迹和视频。
7. 最终审计：报告单机连续墙面回球能力及局限。必要时第 8–10 轮解决证据缺口。若原高层无法直接单机运行，明确报告架构限制与适配结果，而非把未适配失败视为技能无能力。

## 第 1 轮：完成
- 原始运行时仓库：`third_party/HCSP`，使用 `Coselfplay_Phase_two` 环境和 `PSROPolicy_coselfplay_phase_two`。
- 原始 shell 参数：IrisTest，dt=0.01，substeps=1，random_turn=true。
- 我方高层：`checkpoint/coselfplay/1.pt`；旧高层：`checkpoint/high_level_old/5.pt`；新增二传技能：`checkpoint/coselfplay/checkpoint_final_SecPass.pt`。
- 保留 6 架无人机、全部技能及原始初始化和终止逻辑。16 环境初始基线只用于运行与调用链验证，尚不是能力结论。
- 脚本：`scripts/probe_hcsp_full.py`；首轮日志：`runs/hcsp-full-baseline-01.log`。

### 入口核验发现与修正
- `hcsp-full-baseline-01` 明确失败于首次推理：阶段二高层动作头为 `(6,4,4)` 共 14 维，而包括 `coselfplay/1.pt`、`policy_high_level_final.pt` 在内的全部已提供高层权重是 13 维。没有扩展或随机填补权重。
- 阶段一环境的动作头为 `(6,3,4)` 共 13 维，且使用完整高层 + 26 个低层角色实例；共自博弈最终二传权重对应阶段一的 `SecPass` 技能槽。
- 第二次基线改用完整阶段一入口，并从 `policy_high_level_final.pt` 加载双方 actor、critic、归一化参数，从 `checkpoint_final_SecPass.pt` 加载我方二传，其余角色按原始阶段一 shell 配置加载。保留原始六机环境，无墙、无学习。
- 原始 `racket_hit_ball` 是邻近判定结合速度变化的触球代理，不能直接称为接触力确认的真实触球。第 2 轮须加入独立接触验证。

- `baseline-02` 因测试脚本把高层路径误分派给低层加载函数而失败，已修正为独立加载高层；该错误不是策略表现。`baseline-03` 正在运行。
- 源码检查发现接发球悬停分支可能存在赋值笔误（`FirstPass_receive_action = torch.where(...)`，最终执行变量为 `FirstPass_real_action`）。原始基线保持源码不变；如影响表现，后续通过单项修复对照验证，不能默默修复后称原版。

### 第 1 轮结果
`hcsp-full-baseline-03` 进程退出 0 且报告 passed，16 个首回合全部记录。完整阶段一策略调用链已运行，尚未证明原论文性能已复现。

- 13/16 回合出现原始触球代理事件；总计 18 次，单回合最多 3 次。
- 12/16 回合球穿过 x=0 网平面；这不等于合法过网或对侧成功接球。
- 终止：8 次无人机触地、6 次错误部位触球、2 次球触地（原始环境判定）。
- 高层输出包含多个不同技能组合，不是固定规则替代网络。
- 基线表现尚弱，不能拿它直接否定壁球迁移。下一轮优先检查确定性推理、原始控制步长和独立接触数据，并保留原始运行作为对照。
- 审计摘要：`docs/hcsp-full-baseline-03-summary.json`。原始轨迹及权重 SHA256 见运行报告。未训练或更新任何权重。

## 第 2 轮：完成运行配置与独立接触诊断

两次新运行均 16 个首回合，全部冻结权重，进程退出 0 且报告 passed。

| 配置 | GPU 机体接触回合 | 双方先后接触回合 | 原始触球代理单回合最大值 |
|---|---:|---:|---:|
| 确定性低层，10 ms | 15/16 | 5/16 | 2 |
| 确定性低层，20 ms | 13/16 | 3/16 | 2 |

这些接触包含终止时的错误部位接触，不是合法连续接球成绩。数据从逐物理步 GPU 接触缓冲区读取，记录冲量、接触位置、法向和无人机编号，并检查缓冲区未满。没有单机墙面回合，因此本轮不产生壁球成功率。

结果表明，单独改变确定性推理或步长并未修复原排球基线。根据用户追加的“只需要单机”约束，到此停止扩大六机复现实验，下一轮直接处理一架物理无人机的完整技能库/高层调度映射。保留 10 ms 原完整系统步长作为起点，20 ms 数据作为配置敏感性对照；不得把原排球基线的失败外推为单机壁球不可行。

证据：`runs/hcsp-full-deterministic-01.{json,npz,exit.json}`、`runs/hcsp-full-dt02-01.{json,npz,exit.json}`，摘要保存在 `docs/` 同名 `-summary.json`。两次测试均已终止，无遗留训练进程。

## 第 3 轮：单机物理适配与首轮事件审计

已实现 `hcsp_single_wall_env.py`、`probe_hcsp_single_wall.py` 及独立审计 `audit_hcsp_single_wall.py`。每环境真实 articulation shape 为 `(16,1)`，六个状态槽属于网络适配，不是额外物理无人机。全部 26 个低层角色权重及完整高层 checkpoint 已加载，SHA256 已核验。权重未训练或更新。

- 第一次运行 `hcsp-single-wall-01` 因评测接口缺少奖励字段失败；补齐零奖励字段后重跑，未改动作网络。
- `hcsp-single-wall-02`：16 个首回合，10 ms 步长，确定性推理。进程和 JSON 均 passed。
- 16/16 球与机体接触；16/16 发生实体墙碰撞；11/16 出现机体→墙→同机接触，最多 1 次。
- 代表场景 0：第 66 步首次击球，第 135 步在墙前 x≈0.22 m 碰撞，第 190 步在 x≈3.88 m 再次接触同机。由物理轨迹与分别过滤的 GPU 接触冲量确认；回合中未改写球状态。
- **这不是 11/16 合法垫球成功率**：例如场景 0 返回时球中心低于机体中心，可能是下方/侧面碰撞。接触点与机体法向分类尚未完成。
- 本次实际执行角色全部为 FirstPass（3972 个有效控制步）；高层/其它技能虽运行，但没有被仲裁选中执行。不能据此声称完整技能库已经在壁球中发挥了作用，也不能把其余角色未执行解释为无能力。
- 适配定义及假设见 `docs/hcsp-single-wall-adapter.md`：同机状态复制、镜像对手占位、单动作仲裁、墙面回合重置均明确列出。

下一轮：保存实际技能名称以及球—机/墙接触点和法向，审计合法碰撞，检查单机角色仲裁及墙面回合切换；对照保留原始高层输出与适配调度，不把身体碰撞计为合法壁球成绩。

## 第 4 轮：接触几何与实际技能审计

新增接触点/法向、回合编号、全局步号及实际技能记录。`hcsp-single-wall-contact-02` 进程与报告 passed，独立事件链与几何审计 passed。与第 3 轮的初始状态完全相同，保存的球和无人机轨迹最大差值均为 0，证明此次仪表化没有改变该对照轨迹。

- 仍为 16/16 撞墙、11/16 同机再次接触、最多一次。
- 其中 **7/16 是原生机体碰撞箱顶面接球候选**，另外 4 个回球接触未满足顶面几何标准。
- 标准：接触点在机体坐标 z>0，法向绝对 z 分量≥0.9，x/y 在原生箱体半宽 0.235 m 加 0.02 m 运动容差内；墙接触点需在 x=0.1±0.06 m、法向绝对 x 分量≥0.9。阈值及逐事件数值已保存。
- 不能称为 AeroWall 合法球拍率：IrisTest 球拍只作视觉展示，实际基体是碰撞箱；接触点用步末姿态变换，尚非精确碰撞时姿态，也未审计旋翼接触。
- 实际技能控制步：发球 1064、发球后悬停 1074、一传 1700、一传悬停 134。其余技能本轮未取得执行权。
- 墙后 1850 控制步中，1101 步高层选择多个击球角色；FirstPass 优先的单机仲裁压制了同时被选中的二传或进攻角色。这是当前适配的具体限制，不能归因于原技能没有能力。

下一轮保持网络与初始化不变，对照角色仲裁顺序，检验实际执行二传/进攻时的单机墙面结果。随后扩展种子、保存轨迹可视化并完成单机范围的最终审计。

证据：`docs/hcsp-single-wall-contact-02-audit.json`、`docs/hcsp-single-wall-contact-02-classification.json`、`docs/hcsp-single-wall-round4-comparison.json`。
